form#form-task.form-inline(class='survey')
  .form
    .main
      input(type='text', id='content', name='content', placeholder='설문지 제목이 무엇인가요?')
      input(type='text', id='content', name='content', placeholder='질문 제목이 무엇인가요?')
    .sub
      h4 질문 유형이 무엇인가요?
      .form-group
        select.form-control(name='category')
          option(value='선택', selected) 선택
          option(value='객관식') 객관식
          option(value='주관식') 주관식
          option(value='의견 묻기') 의견 묻기
          option(value='기타') 기타
      h4 중요도는 어떤가요?
      .form-group
        select.form-control(name='priority')
          option(value='0', selected) 선택
          option(value='3') 중요
          option(value='2') 보통
          option(value='1') 사소


          
  .actions
      button#post-task.btn.btn-primary.btn-lg.btn-block(type='button') 저장

ul.tasks

.no-task 
  .alert.alert-info 설문지가 없습니다.
script#task-template(type='text/template').
  <li class='task <%= doneStr %>' data-id='<%= id %>'>
    <span class='check'>
      <i class='fa fa-check fa-lg'></i>
    </span>
    <span class='content'><%=content %></span>
    <% if (category) { %>
      <span class='label label-default category'> 
        <%= category %>
      </span>
    <% } %>
    <span class='priority p<%= priority %>'>
      <%= (priority === 3) ? "중요" : ((priority === 1) ? "사소": "보통") %>
    </span>
    <a href='#' class='remove'><i class='fa fa-trash-o'></i></a>
  </li>
      
script.
  $(function() {
    var survey = new TaskController();    
  });
